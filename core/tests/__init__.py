"""Test package for the `core` app.

Layout (filled in by later tasks):

- ``conftest.py``   shared fixtures, including the settings-override hook used to
  inject the fake payment gateway
- ``strategies.py`` shared hypothesis strategies
- ``factories.py``  model factories
- ``test_*.py``     one module per property / unit-test group
"""
