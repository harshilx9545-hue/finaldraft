# Core
# Range rather than an exact pin: the code is verified against 5.2.x and the
# declared range now agrees with the targeted major version (6.13).
Django>=5.2,<6.0
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
# Declared for parity with the spec's dependency set. Environment reading itself
# is done by gymapp/config.py, which owns the startup validation contract.
django-environ==0.11.2

# Payments
razorpay==1.4.2

# Database (PostgreSQL in production; SQLite needs nothing extra)
psycopg[binary]==3.2.3

# Deferred phases - kept from the original plan, not yet wired
django-htmx
cloudinary
django-cloudinary-storage
celery
redis
twilio

# Tests
pytest==8.3.3
pytest-django==4.9.0
# Needed by the concurrency properties (20, 32), which fan out across processes.
pytest-xdist==3.6.1
# The [django] extra supplies from_model()/register_field_strategy for the
# model-backed strategies in core/tests/strategies.py.
hypothesis[django]==6.112.1
freezegun==1.5.1
